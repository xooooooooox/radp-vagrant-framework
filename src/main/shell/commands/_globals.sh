#!/usr/bin/env bash
# Application-level global options for radp-vf
# These options are available to all commands
#
# Variables: gopt_config, gopt_env

# @global -c, --config <dir> Configuration directory
# @global -e, --env <name> Override environment name
